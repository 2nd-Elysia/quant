问题定义 → 数据 → 探索 → 切分 → 特征 → 模型 → 训练 → 评估 → 诊断 → 上线监控。默认以**时序预测 forecasting**为主，因为这是最常见的时序建模任务；异常检测、分类、插补会在后面补充。

## 0. 先建立总框架：时序建模不是“选个 LSTM/Transformer”

经典预测流程通常包括五步：定义问题、收集信息、探索分析、选择并拟合模型、使用和评估模型。Hyndman 与 Athanasopoulos 的《Forecasting: Principles and Practice》也强调，预测任务最难的往往是**问题定义**，并且在建模前应先图形化分析趋势、季节性、异常点和变量关系。

一个完整时序项目应回答这些问题：

1. **预测什么**：单变量 `y_t`，还是多变量 / 多实体 panel data，比如 `(store_id, sku_id, date, sales)`。
2. **预测多远**：1 步、7 天、28 天、12 个月，还是任意 horizon。
3. **预测频率**：分钟级、小时级、日级、周级、月级。
4. **什么时候预测**：每天 00:00 预测未来 7 天，还是每小时滚动预测。
5. **预测结果如何使用**：补货、调度、告警、交易、容量规划、推荐排序。
6. **错误代价是什么**：高估和低估是否对称？RMSE、MAE、pinball loss、service level、库存缺货率哪个更贴近业务？
7. **未来可用什么信息**：节假日、价格、天气预报、促销计划、日历变量是已知未来变量；销量、流量、点击、传感器读数通常不是未来已知变量。

这一点非常关键：**时序问题的核心约束是“预测时刻能看到什么”，不是数据表里有什么。**

---

## 1. 明确任务类型

### A. 预测 forecasting

最常见：给过去窗口，预测未来值。

形式上可以写成：

$$\hat{y}_{t+1:t+H}=f(y≤t,x≤t,zt+1:t+H)$$

其中：

- `y`：目标序列。
- `x`：历史可观测协变量，比如历史价格、历史温度、历史流量。
- `z`：未来已知协变量，比如节假日、星期几、排班计划、已确定的促销。

### B. 分类 classification

例如 ECG 分类、机器振动状态识别、用户行为序列分类。输入是一段序列，输出一个类别。

### C. 异常检测 anomaly detection

例如检测服务器指标异常、设备故障、欺诈行为。可做点异常、区间异常、上下文异常。

### D. 插补 imputation

补缺失值，比如传感器丢包、医疗监测缺失、金融行情缺口。

### E. 因果 / 干预分析

例如促销是否带来销量提升、政策实施是否改变趋势。这类问题不能只做预测，还需要反事实建模。

## 2. 数据建模：先做“时序数据契约”

最小字段通常是：

```
series_id, timestamp, target_y, known_future_covariates, observed_covariates, static_features
```

如果是单条序列，可以没有 `series_id`；如果是很多商品、门店、用户、设备，就必须有实体 ID。

数据检查重点：

- 时间戳是否单调递增。
- 是否有重复时间点。
- 频率是否固定，例如每天一条、每小时一条。
- 是否有缺失时间段。
- 时区、夏令时、交易日、节假日是否处理正确。
- 目标值是否有零、负数、截断、上限、下限。
- 是否存在系统迁移、口径变化、埋点变化。
- 是否有实体生命周期：新品、下架、停机、用户流失。
- 协变量在预测时刻是否真的可用。

一个非常实用的表是**特征可得性矩阵**：

| 特征     | 训练中有 | 预测时有 | 是否可用于未来 | 备注                   |
| -------- | -------- | -------- | -------------- | ---------------------- |
| 历史销量 | 是       | 是       | 否             | 只能用到 cutoff 之前   |
| 星期几   | 是       | 是       | 是             | 未来已知               |
| 节假日   | 是       | 是       | 是             | 未来已知               |
| 天气实况 | 是       | 否       | 否             | 不能直接用未来实况     |
| 天气预报 | 是       | 是       | 是             | 可用，但有预报误差     |
| 促销计划 | 是       | 是       | 视业务而定     | 若未来促销已排定则可用 |

这一步能提前避免大部分数据泄漏。

---

## 3. EDA：时序探索不是画一张折线图就结束

先做这些图和统计：

1. **原始时间图**：看趋势、季节性、突变、异常、缺失。
2. **按周期聚合图**：小时内、周内、月内、节假日前后。
3. **季节子序列图**：比如每年 12 个月分组看。
4. **ACF/PACF**：看自相关、季节性滞后、周期结构。
5. **STL/季节趋势分解**：拆成 trend / seasonal / remainder。
6. **目标分布图**：是否长尾、零膨胀、间歇需求。
7. **按实体分布**：不同 SKU、门店、设备之间是否尺度差异巨大。
8. **协变量关系图**：价格、促销、温度、工作日对目标的影响。
9. **缺失和异常图**：缺失是否集中在某些时间段或实体。
10. **漂移检查**：训练早期和近期分布是否明显不同。

FPP3 强调建模前要先通过图形理解模式，包括趋势、季节性、周期、异常和变量关系；这不是形式步骤，而是决定后续模型选择和特征工程的基础。

---

## 4. 数据切分：时序不能随机切

时序建模最常见的错误是随机 train/test split。普通交叉验证会把未来信息泄漏到训练中。scikit-learn 的 `TimeSeriesSplit` 文档也明确指出，时间有序数据不适合普通 CV，因为普通方法可能导致“用未来训练、在过去测试”；它采用前面时间段训练、后面时间段测试的方式，并支持 `gap` 参数在训练集和测试集之间留空。

推荐切分方式：

### A. 固定 holdout

```
train: 过去 70%
valid: 中间 15%
test : 最后 15%
```

简单，但只评估一个历史片段，方差较大。

### B. expanding window backtest

```
train [1..100] -> test [101..107]
train [1..107] -> test [108..114]
train [1..114] -> test [115..121]
```

训练集逐步扩大，适合数据稳定、越多历史越好的场景。

### C. rolling window backtest

```
train [1..100]   -> test [101..107]
train [8..107]   -> test [108..114]
train [15..114]  -> test [115..121]
```

训练窗口固定长度，适合分布漂移明显、老数据价值下降的场景。

### D. 带 gap 的 backtest

```
train [1..100] -> gap [101..103] -> test [104..110]
```

适合标签窗口重叠、延迟反馈、金融标签、传感器滞后等场景。

FPP3 对 rolling forecasting origin 也有类似描述：每个测试点只能用它之前的数据训练，且多步预测时应按目标 horizon 评估。

---

## 5. 预处理：所有变换都必须“按时间折内 fit”

### 缺失值

不要无脑全局均值填充。常见策略：

- 短缺口：forward fill、插值、局部均值。
- 长缺口：标记缺失段，甚至丢弃该实体。
- 计数 / 销量：缺失和 0 不是一回事。
- 传感器：缺失本身可能代表设备异常，要加 missing indicator。

注意：**不能用未来值填过去缺失**，除非你的真实预测场景也允许离线平滑。

### 异常值

先区分：

- 数据错误：采集失败、重复上报、单位变了。
- 真实事件：大促、疫情、断电、政策冲击。
- 极端但可预测：节假日峰值、天气极端。
- 极端且不可预测：黑天鹅。

真实事件不一定要删。删掉会让模型在真正需要处理峰值时失效。

### 变换

常用：

- `log1p(y)`：处理长尾、方差随均值增大。
- Box-Cox / Yeo-Johnson：稳定方差。
- differencing：处理趋势和非平稳性。
- seasonal differencing：处理季节性。
- winsorization：截尾极端值。
- robust scaling：中位数和 IQR 缩放。

注意逆变换偏差：如果模型在 log 空间预测均值，直接 `exp(pred)` 不一定是原空间均值。

### 标准化

时间序列里标准化尤其容易泄漏：

```
错误：用全量数据 fit scaler
正确：每个 backtest fold 只用 train fit scaler，再 transform valid/test
```

多实体场景下还要决定：

- 每条序列单独标准化：适合尺度差异极大。
- 全局标准化：适合实体可比。
- 按类别标准化：例如按商品类目或设备类型。

## 6. 特征工程：传统 ML 时序建模的核心

在很多业务预测中，**LightGBM / CatBoost + 滞后特征 + 滚动统计 + 日历特征**仍然非常强。M5 这类零售需求预测竞赛也显示，树模型、深度模型和集成在实际大规模层级销量预测中表现突出；但对间歇需求、零膨胀、过度离散等问题，单纯用常规回归目标也可能不充分。

### A. 滞后特征 lag features

```
y_{t-1}, y_{t-2}, y_{t-7}, y_{t-14}, y_{t-28}
```

日级数据常用：

- 昨天。
- 上周同一天。
- 上月附近。
- 去年同一天或同周。

### B. 滚动统计 rolling features

关键原则：**先 shift，再 rolling**。

错误写法：

```python
rolling_mean_7 = y.rolling(7).mean()
```

这可能包含当前时刻目标。

正确逻辑：

```python
rolling_mean_7 = y.shift(1).rolling(7).mean()
```

常用统计：

- rolling mean。
- rolling median。
- rolling std。
- rolling min/max。
- exponentially weighted mean。
- 最近非零间隔。
- 最近一次峰值距离。
- 最近 N 天缺货天数。
- 最近 N 天促销次数。

### C. 差分和增长率

```
y_t - y_{t-1}
y_t / y_{t-7} - 1
rolling_mean_7 / rolling_mean_28
```

适合有趋势或尺度变化的序列。

### D. 日历特征

- hour。
- day of week。
- day of month。
- month。
- quarter。
- weekend。
- holiday。
- holiday distance。
- pay day。
- school vacation。
- trading day。
- month start / month end。

周期变量建议用 sin/cos 编码：

 $$\quad sin(2πt/P),cos(2πt/P)$$

例如小时、星期、月份。

### E. 未来已知协变量

非常有价值：

- 价格计划。
- 促销计划。
- 节假日。
- 排班。
- 航班计划。
- 天气预报。
- 生产计划。
- 发布计划。

注意：如果未来价格是模型自己预测出来的，就要把价格预测误差纳入不确定性。

### F. 静态特征

多实体预测常用：

- 商品类目。
- 门店区域。
- 设备型号。
- 用户类型。
- 地理位置。
- 上架时间。
- 历史均值、历史波动、历史稀疏度。

深度模型里通常用 embedding；树模型里可以直接类别编码或 target encoding，但 target encoding 必须按时间滚动计算，不能用全量均值。

## 7. Baseline：没有强 baseline 的复杂模型基本不可信

至少做这些：

1. **Naive**：下一期等于上一期。
2. **Seasonal naive**：下一周同一天等于上周同一天。
3. **Moving average**：最近 N 期均值。
4. **Drift**：线性外推。
5. **ETS / ARIMA**：经典统计基线。
6. **业务规则基线**：例如人工计划、去年同期、预算系统。

残差诊断也很重要。一个好的预测方法应让创新残差近似无自相关、均值为 0；如果残差仍有相关性，说明还有信息没被模型利用。FPP3 也提醒，残差诊断是检查模型是否利用完信息的重要步骤，但不是唯一的模型选择标准。

---

## 8. 模型选择：按数据规模、稳定性、解释性和 horizon 选

### A. 经典统计模型

适合：单条或少量序列、数据不大、需要解释、季节性明确。

常见模型：

- Naive / seasonal naive。
- Exponential Smoothing / ETS。
- ARIMA / SARIMA。
- SARIMAX / dynamic regression。
- State space model。
- VAR / VECM。
- Prophet 类加性模型。
- STL + 模型。
- Gaussian Process，适合小数据和不确定性表达。

优点：

- 小数据强。
- 可解释。
- 不容易过拟合。
- 训练快。
- 预测区间成熟。

缺点：

- 难以利用大量异构实体。
- 非线性和复杂协变量处理能力有限。
- 对异常和结构突变敏感。

### B. 机器学习表格模型

适合：多实体、多协变量、业务特征丰富。

常见模型：

- Linear / Ridge / Lasso。
- Random Forest。
- XGBoost。
- LightGBM。
- CatBoost。
- ExtraTrees。
- NGBoost / quantile regression forest。

建模方式：把时序转成监督学习表。

```
features at time t  ->  target y_{t+h}
```

优点：

- 特征工程后非常强。
- 训练成本低。
- 可解释性较好。
- 对 tabular covariates 友好。
- 多实体 global model 效果好。

缺点：

- 长期依赖需要人工 lag/rolling 设计。
- 多步预测策略要手动设计。
- 预测区间需要额外处理。

### C. 深度学习模型

适合：大量序列、复杂非线性、多变量、多步预测、需要 global model。

常见模型：

- RNN / LSTM / GRU。
- TCN。
- DeepAR。
- N-BEATS / N-HITS。
- TFT。
- Informer / Autoformer / FEDformer。
- PatchTST。
- TiDE。
- TimesNet。
- iTransformer。

N-BEATS 用全连接残差结构做单变量预测，并在多个经典数据集上展示强性能；TFT 处理多 horizon 场景，区分静态变量、历史变量和未来已知变量，并包含变量选择和注意力解释机制；PatchTST 则把时间序列切成 patch，并采用 channel independence 以降低注意力计算量、增加可看的历史长度。

优点：

- global learning 强。
- 可直接做多步预测。
- 可做概率预测。
- 可学习复杂非线性。
- 对大规模 panel data 友好。

缺点：

- 数据量要求高。
- 调参成本高。
- 可解释性较弱。
- 对数据泄漏和评估设置敏感。
- 小数据上不一定比 ETS/ARIMA/树模型强。

另外，不要迷信 Transformer。DLinear 论文曾指出，在一些 long-term forecasting benchmark 上，简单线性模型可以超过当时复杂 Transformer 模型；这提醒我们时序预测中趋势、季节性、评估方式和归一化常常比架构名更关键。

### D. 时序基础模型 / foundation models

近年出现了 TimeGPT、TimesFM、Chronos、Moirai、Time-MoE 等预训练时序模型。TimeGPT 主打跨数据集 zero-shot 预测；TimesFM 是 decoder-only patched attention 模型；Chronos 把时序值缩放、量化成 token，用语言模型式目标训练；Moirai 试图做跨频率、跨变量数、跨分布的通用预测模型。

实用建议：

- 可以把 foundation model 当作强 baseline。
- 数据少、冷启动、快速原型时很有价值。
- 不要默认它能超过针对业务调好的 LightGBM / TFT / N-BEATS。
- 对结构突变、分布漂移、强业务协变量场景仍要谨慎。
- 生产中最好和传统 baseline、业务规则、残差校准、集成一起用。

已有研究也指出，时序基础模型的 zero-shot 能力受预训练领域影响；在真实未见序列和冲击期，未必稳定优于小型专用模型。

---

## 9. 多步预测策略

假设要预测未来 `H` 步。

### A. Recursive

训练一步模型：

```
y_{t+1} = f(history)
```

然后递归预测：

```
预测 y_{t+1}
用预测的 y_{t+1} 再预测 y_{t+2}
...
```

优点：简单，样本多。
 缺点：误差累积。

### B. Direct

每个 horizon 训练一个模型：

```
model_h1 -> y_{t+1}
model_h2 -> y_{t+2}
...
model_hH -> y_{t+H}
```

优点：不会递归误差累积。
 缺点：模型多，远期样本少。

### C. Multi-output

一个模型同时输出未来 H 步：

```
[y_{t+1}, ..., y_{t+H}] = f(history)
```

优点：直接优化多步。
 缺点：模型更复杂，对数据量要求更高。

### D. Seq2Seq / decoder

深度模型常用，适合长 horizon、多变量、多实体。

实务上：

- 短 horizon：recursive 可以先试。
- 中长 horizon：direct 或 multi-output 更稳。
- 业务不同 horizon 权重不同：训练 loss 和验证 metric 都要加权。
- 不同 horizon 误差要分开看，不要只看平均值。

---



## 10. 损失函数和指标：别只看一个 RMSE

常用点预测指标：

- **MAE**：鲁棒、可解释。
- **RMSE**：更惩罚大误差。
- **MAPE**：直观，但目标为 0 或接近 0 时会失效。
- **sMAPE**：常见，但也有不稳定问题。
- **MASE / RMSSE**：适合跨序列比较。
- **WAPE**：业务常用，但可能被大规模实体主导。
- **自定义成本函数**：库存、缺货、过载、告警误报等。

FPP3 明确指出，MAPE 在真实值为 0 时会无限或未定义，真实值接近 0 时会极端；它也推荐用 scaled errors，如 MASE/RMSSE，来跨不同尺度序列比较。

概率预测指标：

- quantile loss / pinball loss。
- CRPS。
- coverage。
- interval width。
- calibration curve。
- PICP / MPIW。

工程上应同时看：

```
整体指标
分 horizon 指标
分实体指标
分时间段指标
分业务场景指标
峰值期指标
低销量 / 低流量指标
冷启动实体指标
异常期指标
```

一个模型整体 RMSE 低，不代表在高峰、冷启动、新品、大促、异常天气下表现好。

---

## 11. 训练与调参注意点

### A. 所有调参必须嵌入 backtest

错误：

```
在 test 上反复调参
```

正确：

```
train -> valid/backtest 调参
最后只在 test 上评估一次
```

### B. early stopping 的验证集也必须按时间切

不能随机抽验证集。

### C. 多实体训练要处理尺度差异

常用方法：

- per-series normalization。
- log transform。
- static embedding。
- sample weighting。
- entity-level balanced sampling。
- 对低销量实体和高销量实体分层建模。

### D. 数据窗口长度要实验

过短：看不到季节性。
 过长：老数据引入漂移。
 常见 trick：

```
lookback = max(2~4 个季节周期, 业务记忆长度)
```

例如周季节日级数据至少看 28、56、84 天；年季节月级数据至少看 24、36、60 个月。

### E. 样本权重

可以按：

- 近期更高权重。
- 高价值 SKU 更高权重。
- 峰值期更高权重。
- 缺货期降低权重或特殊处理。
- 不同 horizon 不同权重。

### F. 集成通常很有效

M4/M5 这类预测竞赛反复说明，组合、混合和集成方法经常非常强。M4 中高排名方案大量使用模型组合；M5 中树模型、深度模型和集成方法都非常突出。

实用集成方式：

- naive + ETS + LightGBM。
- LightGBM 多组 lag window 集成。
- 统计模型预测作为 ML 特征。
- 深度模型 + 树模型平均。
- 不同随机种子平均。
- 按验证集表现加权。
- residual model：先用主模型，再建模残差。

---

## 12. 最常见的坑

### 1. 未来信息泄漏

典型泄漏：

- rolling mean 没有 shift。
- scaler 用全量数据 fit。
- target encoding 用了未来标签。
- 缺失值插补用了未来。
- 特征里包含发布后才知道的数据。
- 随机切分。
- 同一个事件窗口既出现在训练又出现在测试。
- 用未来实际天气，而不是天气预报。
- 用未来实际库存，而不是预测时刻知道的库存计划。

判断标准很简单：**在预测时刻 cutoff，你能不能拿到这个值？**

### 2. 预测目标定义不清

例如：

- 预测订单量还是发货量？
- 缺货时销量是需求还是供应限制？
- 退款、取消、延迟上报如何处理？
- 使用自然日还是营业日？
- 预测 GMV、件数、用户数还是转化率？

目标口径错了，模型再强也没用。

### 3. 把异常全删掉

如果异常是未来也会出现的业务事件，比如大促、节假日、极端天气，删掉会让模型失去处理峰值的能力。

### 4. 只看平均误差

平均误差会掩盖：

- 远期 horizon 崩掉。
- 长尾实体崩掉。
- 高峰期崩掉。
- 新品崩掉。
- 某个区域崩掉。
- 低值区间 MAPE 爆炸。

### 5. MAPE 滥用

目标有 0 或接近 0 时不要主用 MAPE。销量、点击、故障数、告警数、交易量经常有 0，建议看 MAE、RMSE、WAPE、MASE/RMSSE、pinball loss 或业务指标。

### 6. 忽略层级一致性

例如：

```
单品销量预测加总 ≠ 类目销量预测
门店预测加总 ≠ 大区预测
```

层级预测要考虑 reconciliation。FPP3 也专门将 hierarchical / grouped time series 和 forecast reconciliation 作为实践主题。

### 7. 忽略分布预测

业务决策常常需要区间而不是点预测：

- 库存：需要 P90/P95。
- 容量：需要高分位。
- 金融风险：需要尾部风险。
- 告警：需要置信度。
- 供应链：需要 service level。

点预测准，不代表风险可控。

### 8. 训练分布和上线分布不同

上线后会遇到：

- 新品。
- 新门店。
- 新设备。
- 大促策略变化。
- 埋点改版。
- 政策变化。
- 外部冲击。
- 用户行为变化。

所以要监控数据漂移、特征缺失率、预测分布、残差分布、分 horizon 误差。

---

## 13. 上线闭环：预测系统要记录“当时知道什么”

生产系统最重要的不是模型文件，而是可复现性。

必须记录：

```
forecast_time / cutoff_time
训练数据版本
特征版本
模型版本
预测 horizon
输入特征快照
预测值
预测区间
真实值回填时间
误差
异常标记
人工覆盖记录
```

上线后要监控：

- 数据延迟。
- 特征缺失率。
- 目标回填延迟。
- 预测分布漂移。
- 残差均值是否偏离 0。
- 残差自相关。
- 分 horizon 误差。
- 分实体误差。
- prediction interval coverage。
- 业务指标改善。
- 人工 override 比例。

还要设计 fallback：

```
主模型失败 -> LightGBM baseline
baseline 失败 -> seasonal naive
数据缺失 -> 规则预测
异常事件 -> 人工/业务规则覆盖
```

---

## 14. 建模决策速查

### 数据少、单条序列

优先：

```
naive / seasonal naive / ETS / ARIMA / STL + ETS
```

不要一开始就上 Transformer。

### 多条相似序列，有丰富业务特征

优先：

```
LightGBM / CatBoost + lag/rolling/calendar/static features
```

这通常是最稳的工业 baseline。

### 多变量、多 horizon、大量历史

可以试：

```
TFT / DeepAR / N-BEATS / N-HITS / PatchTST / TiDE
```

### 强季节性 + 可解释要求

试：

```
ETS / SARIMA / Prophet-like / dynamic harmonic regression
```

### 需要不确定性

试：

```
quantile regression
DeepAR
TFT quantile loss
conformal prediction
bootstrap ensemble
distributional models
```

### 冷启动或快速原型

试：

```
TimeGPT / Chronos / TimesFM / Moirai 等 foundation model
```

但要和业务 baseline 做 backtest，不要只看 demo 效果。

### 异常检测

常用：

```
STL decomposition + residual threshold
robust z-score
Isolation Forest on rolling features
autoencoder
forecasting residual anomaly
change point detection
```

### 分类任务

常用：

```
ROCKET / MiniROCKET
1D CNN / TCN
RNN
Transformer encoder
shapelet methods
handcrafted features + tree model
```

---

## 15. 推荐你按这个模板做一个完整项目

一个标准时序预测项目可以这样落地：

```
1. 定义 target、horizon、frequency、cutoff、metric
2. 建立 as-of 数据集，保证每行只包含预测时刻可见信息
3. 做 EDA：趋势、季节、异常、缺失、分实体分布
4. 建 naive / seasonal naive baseline
5. 做 rolling / expanding backtest
6. 构造 lag、rolling、calendar、static、known future features
7. 训练 LightGBM/CatBoost global model
8. 对比 ETS/ARIMA 或 foundation model baseline
9. 如果数据足够，再试 N-BEATS/TFT/PatchTST
10. 分 horizon、分实体、分场景评估
11. 做残差诊断和预测区间校准
12. 做模型集成或残差修正
13. 固化 feature pipeline，避免泄漏
14. 上线记录 cutoff、输入快照、预测、真实回填
15. 监控漂移、误差、coverage、业务指标
```

最核心的经验可以压缩成一句话：

**时序建模的胜负通常不在“模型名字”，而在 cutoff 定义、数据可得性、时间切分、泄漏控制、horizon 评估、特征工程、残差诊断和上线监控。**