# 量化基础知识

这份笔记的定位：不是替代 `01-08` 的详细流程，而是补齐做量化研究时必须先有的底层常识、思维框架和常见 insight。

核心主线：

```text
市场数据 -> 信号/因子 -> 预测 -> 组合 -> 回测 -> 风控 -> 执行 -> 实盘反馈
```

量化不是“用模型预测涨跌”这么简单。更准确地说，量化是在高噪声、低信噪比、强竞争、强约束的金融市场里，把一个投资想法变成可检验、可交易、可风控的系统。

---

## 01. 先建立几个基本判断

### 1. 价格不是重点，收益率才是重点

股票 A 从 10 元涨到 11 元，股票 B 从 100 元涨到 101 元，价格变化都是 1 元，但收益率完全不同：

```text
A: 10%
B: 1%
```

量化研究一般关心收益率、超额收益率、波动率、回撤，而不是价格绝对值。

### 2. 预测方向不等于赚钱

一个模型如果预测明天涨跌方向 52% 正确，看起来比随机强，但仍然可能亏钱，因为：

- 错的时候亏得更多。
- 对的时候收益太小，不够覆盖成本。
- 买不到、卖不出、成交价不现实。
- 收益来自某个风险暴露，而不是模型本身。

量化最终关心的是：

```text
预测信号 -> 仓位 -> 扣成本后收益 -> 风险调整表现
```

### 3. 统计显著不等于经济显著

某个因子的 IC 显著为正，只说明它可能有预测信息；但它是否值得交易，还要看：

- 收益幅度是否够大。
- 换手是否太高。
- 交易成本是否吃掉 alpha。
- 容量是否足够。
- 是否只在极端样本里有效。

金融里常见问题是：统计上看起来有一点点规律，交易上完全不够用。

### 4. 回测漂亮不代表策略真实有效

一个漂亮回测可能来自：

- 未来函数。
- 幸存者偏差。
- 数据清洗错误。
- 成本低估。
- 调参过度。
- 样本内过拟合。
- 流动性假设不现实。

看到高收益、高夏普的回测，第一反应应该是怀疑数据和假设，而不是相信策略。

---

## 02. 金融市场最基础对象

### 1. 资产

常见可交易资产包括：

- 股票：买公司所有权的一部分。
- 债券：借钱给发行人，收利息和本金。
- 期货：约定未来买卖某种标的。
- 期权：拥有未来买入或卖出的权利。
- 基金/ETF：一篮子资产。
- 外汇：不同货币之间的兑换。
- 商品：黄金、原油、铜、农产品等。

入门量化通常先从股票或 ETF 开始，因为数据更直观，概念也更容易串起来。

### 2. 股票价格与复权

股票会分红、送股、拆股。直接用原始价格计算收益，可能会把分红除权误认为暴跌。

所以做历史收益时通常要用复权价格：

- 前复权：把历史价格调整到当前口径。
- 后复权：把当前价格调整到历史口径。
- 不复权：保留真实交易价格，适合模拟成交价。

重要 insight：研究收益和因子时常用复权价，但模拟真实成交时要注意成交价不能直接用复权后的虚拟价格。

### 3. 收益率

简单收益率：

```text
r_t = P_t / P_{t-1} - 1
```

对数收益率：

```text
log_return_t = log(P_t / P_{t-1})
```

简单收益率更符合实际盈亏；对数收益率在时间序列建模里更方便，因为多期可以相加。

### 4. 超额收益

很多策略不是和 0 比，而是和基准比：

```text
超额收益 = 策略收益 - 基准收益
```

例如指数增强策略的目标不是“绝对赚钱”，而是跑赢沪深 300、中证 500 或中证 1000。

### 5. 仓位和权重

如果有 N 个资产，每个资产权重是 `w_i`，组合收益为：

```text
r_p = sum(w_i * r_i)
```

量化的很多问题最后都会落到一个问题：

```text
每个资产买多少？
```

预测只是中间环节，仓位才会真正产生收益和风险。

---

## 03. 收益、风险和评价指标

### 1. 年化收益

年化收益是把不同长度的策略表现换成可比口径。日频粗略估算：

```text
年化收益 ≈ 日均收益 * 252
```

更严谨时用复利：

```text
年化收益 = (期末净值 / 期初净值)^(252 / 交易日数) - 1
```

### 2. 年化波动率

日收益标准差年化：

```text
年化波动率 = 日收益标准差 * sqrt(252)
```

波动率越高，收益越不稳定，但波动本身不一定坏。关键是收益是否足以补偿波动。

### 3. 夏普比率

```text
Sharpe = 年化超额收益 / 年化波动率
```

它衡量每承担一单位波动，能获得多少收益。面试中要能说清楚：夏普高不一定代表绝对收益高，而是风险调整收益较好。

### 4. 最大回撤

最大回撤衡量从历史高点跌到低点的最大亏损：

```text
Drawdown_t = NAV_t / max(NAV_1...NAV_t) - 1
```

回撤比波动更接近投资者真实痛感。很多策略不是死于长期收益不好，而是死于中途回撤过大。

### 5. 信息比率

```text
IR = 年化超额收益 / 年化跟踪误差
```

信息比率常用于评价相对基准策略，比如指数增强。Sharpe 看绝对收益质量，IR 看相对收益质量。

### 6. 胜率、赔率和期望

胜率高不等于赚钱。关键是期望收益：

```text
期望 = 胜率 * 平均盈利 - 亏损概率 * 平均亏损
```

一个策略 40% 胜率也可能赚钱，只要盈利时赚得足够多；一个 70% 胜率策略也可能亏钱，如果亏损时亏得很大。

---

## 04. 信号、因子和 alpha

### 1. 信号是什么

信号是一个变量，它对未来收益有一定预测力。

例子：

- 过去 20 天收益率：动量或反转信号。
- 市盈率、市净率：价值信号。
- ROE、毛利率、现金流：质量信号。
- 成交量变化、换手率：量价信号。
- 新闻情绪、分析师预期变化：情绪或预期信号。

### 2. 因子是什么

在股票量化里，因子通常就是可以横向比较股票的特征。每天对每只股票计算一个值：

```text
x_{t,i}
```

其中 `t` 是日期，`i` 是股票。

### 3. Alpha 是什么

Alpha 可以理解为无法被常见风险因子解释的超额收益。更直白地说：

```text
策略真的靠自己的信号赚到的钱
```

但实务中要谨慎：很多看起来像 alpha 的收益，其实来自小盘、行业、市场 beta、流动性风险或杠杆。

### 4. 常见因子大类

- 价值：低估值股票长期可能有补偿。
- 动量：过去强的股票未来一段时间继续强。
- 反转：短期过度上涨或下跌后反向修复。
- 质量：盈利稳定、现金流好、杠杆低。
- 规模：小市值可能有风险溢价，但容量和流动性差。
- 低波动：低波股票在某些市场中风险调整收益更好。
- 流动性：低流动性资产可能有补偿，但交易难。
- 情绪：新闻、研报、资金流、社媒情绪等。

### 5. 信号评价不只看 IC

一个因子需要同时看：

- IC 均值和稳定性。
- RankIC。
- 分组收益是否单调。
- 多空组合收益。
- 换手率。
- 行业和市值暴露。
- 样本外表现。
- 扣成本后表现。

核心 insight：一个好因子不一定每天都有效，但应该在长期、不同市场环境、不同股票池中有稳定倾向。

---

## 05. 数据层基础

### 1. 常见数据类型

- 行情数据：开高低收、成交量、成交额、涨跌幅。
- 财务数据：利润表、资产负债表、现金流量表。
- 估值数据：PE、PB、PS、EV/EBITDA。
- 公司行为：分红、拆股、停复牌、增发、回购。
- 指数成分：某日期指数包含哪些股票。
- 行业分类：申万、中信、GICS 等。
- 高频数据：逐笔成交、订单簿、委托撤单。
- 另类数据：新闻、舆情、招聘、供应链、卫星图像等。

### 2. 数据对齐很关键

量化里最容易出错的是时间戳。原则是：

```text
在 t 时刻做决策，只能使用 t 时刻已经知道的信息
```

财报的会计期末日期不是可用日期。比如 2025 年年报的报告期是 2025-12-31，但实际披露可能在 2026-03-30。回测中必须按披露日可用。

### 3. 股票池要合理

股票池常见过滤：

- 去掉 ST、退市整理、长期停牌。
- 去掉上市时间太短的股票。
- 去掉流动性太差的股票。
- 按指数成分或全市场定义样本。

注意：如果用今天的指数成分回测过去，会产生未来函数。必须使用历史成分股。

### 4. 缺失值不是小事

缺失值可能有信息。例如某公司没有分析师覆盖，本身就说明关注度低。处理缺失值要分清楚：

- 真缺失。
- 尚未披露。
- 不适用。
- 数据供应商错误。

盲目填 0 很危险，因为 0 可能有实际金融含义。

---

## 06. 因子处理基础

### 1. 去极值

极端值会严重影响均值、标准差和回归结果。常见做法：

- 分位数截尾。
- MAD 去极值。
- Winsorize。

### 2. 标准化

截面标准化常见形式：

```text
z_{t,i} = (x_{t,i} - mean_t(x)) / std_t(x)
```

这样不同因子能放在同一量纲下比较。

### 3. 中性化

中性化是为了剥离不想要的暴露。比如行业中性化、市值中性化：

```text
factor = a + b1 * industry + b2 * log_market_cap + residual
```

用残差作为处理后的因子。

重要 insight：中性化不是永远越多越好。它会降低某些风险暴露，也可能去掉本来有价值的信息。

### 4. 因子方向统一

多个因子合成前，要保证高分都代表“更看好”。例如：

- ROE 越高越好。
- PE 如果做价值因子，通常越低越好，需要取负或倒数。

否则因子合成会互相抵消。

---

## 07. 建模基础

### 1. 量化预测常见目标

- 预测未来收益率。
- 预测未来超额收益。
- 预测未来排名。
- 预测波动率。
- 预测成交量或流动性。
- 预测风险暴露或违约概率。

选股里很多时候更关心排序，而不是精确预测收益数值。

### 2. 为什么不能随机切分数据

金融数据有时间顺序。训练集、验证集、测试集必须按时间切：

```text
过去训练 -> 未来验证 -> 更未来测试
```

随机切分会让模型间接看到未来市场状态，造成时间泄漏。

### 3. 常见模型

- 线性回归：简单、可解释、稳定。
- Ridge/Lasso：用正则化控制过拟合。
- Logistic/分类模型：预测上涨概率或分组。
- GBDT/XGBoost/LightGBM：处理非线性和交互。
- 神经网络：适合高频、文本、多模态或复杂时序。
- 时间序列模型：ARIMA、GARCH、状态空间模型等。

### 4. 普通 ML 指标和量化指标不一致

普通 ML 常看：

```text
MSE, MAE, Accuracy, AUC
```

量化更关心：

```text
IC, RankIC, Sharpe, IR, Drawdown, Turnover, Cost
```

模型误差小，不代表能赚钱；排序能力稳定、成本可控，反而更重要。

---

## 08. 组合构建基础

### 1. 从预测到仓位

模型输出预测收益或评分后，还要决定每只股票买多少：

```text
score -> expected return -> portfolio weight
```

不能简单“分数最高就全买”，因为会带来集中度、行业暴露、流动性、换手和风险问题。

### 2. 均值-方差思想

经典形式：

```text
max w'μ - λ/2 * w'Σw
```

含义：

- `w'μ`：组合预期收益。
- `w'Σw`：组合风险。
- `λ`：风险厌恶程度。

### 3. 实盘约束

常见约束包括：

- 权重和为 1。
- 单票最大权重。
- 行业偏离限制。
- 风格暴露限制。
- 换手率限制。
- 流动性限制。
- 禁止买入停牌、涨停或风险警示股票。

重要 insight：组合优化不是为了让数学形式更高级，而是为了把预测信号放进真实可交易约束里。

---

## 09. 回测基础

### 1. 回测时间线

正确时间线大致是：

```text
t 日收盘后产生信号
t+1 日交易
t+1 到 t+H 持有并计算收益
```

如果用 t 日收盘价产生信号，又假设 t 日收盘价成交，通常就有不可实现问题。

### 2. 回测必须扣成本

成本包括：

- 手续费。
- 印花税。
- 买卖价差。
- 滑点。
- 市场冲击。
- 借券成本。

一个信号预测收益 0.2%，交易成本 0.3%，这个信号即使方向正确也不能交易。

### 3. 稳健性比单次结果重要

需要看：

- 分年份表现。
- 分市场环境表现。
- 分行业表现。
- 分市值区间表现。
- 参数扰动后的表现。
- 样本外表现。

一个策略只在某一年、某个参数、某个股票池里有效，可信度很低。

---

## 10. 风险与归因基础

### 1. 风险来源

策略收益可能来自：

- 市场 beta。
- 行业暴露。
- 市值暴露。
- 价值、动量、低波等风格因子。
- 流动性风险。
- 杠杆风险。
- 尾部风险。
- 真正的 alpha。

风险归因就是回答：

```text
策略到底在赚什么钱？
```

### 2. Alpha 和风险模型要区分

Alpha 模型回答：

```text
哪些股票未来更可能跑赢？
```

风险模型回答：

```text
这些股票共同承担了哪些风险？
```

组合优化把两者合在一起：

```text
在风险可控下尽量拿 alpha
```

### 3. 市场中性不是无风险

市场中性只是尽量把市场 beta 控制到 0。它仍然可能有：

- 行业风险。
- 风格风险。
- 流动性风险。
- 做空风险。
- 拥挤交易风险。
- 极端行情风险。

---

## 11. 交易执行基础

### 1. 目标仓位不等于真实成交

策略算出目标权重后，还要变成订单：

```text
目标权重 -> 目标金额 -> 买卖数量 -> 订单 -> 成交 -> 实际仓位
```

每一步都有成本和误差。

### 2. 流动性决定可交易性

一个小盘股因子回测收益很高，但如果成交量很小，大资金无法真实买入。需要看：

- ADV：日均成交额。
- 成交占比。
- 买卖价差。
- 盘口深度。
- 涨跌停和停牌。

### 3. 容量是商业价值核心

策略容量指在不明显恶化收益的情况下最多能管理多少资金。

```text
净收益 = 毛收益 - 成本(资金规模)
```

容量越大，商业价值通常越高。小资金高收益和大资金中等收益是完全不同的策略。

---

## 12. 实盘监控与策略衰减

策略上线后要持续看：

- 实盘收益是否低于回测。
- 滑点是否变大。
- 因子 IC 是否衰减。
- 风险暴露是否漂移。
- 换手率是否异常。
- 持仓是否拥挤。
- 市场环境是否变化。

量化策略不是写完上线就结束，而是长期维护的系统。

常见衰减原因：

- 市场结构变化。
- 竞争者变多。
- 因子拥挤。
- 交易成本上升。
- 数据源或规则变化。
- 模型过拟合被实盘暴露。

---

## 13. 面试里常被问的理解点

### 1. 为什么量化不能只看预测准确率？

因为交易收益取决于收益幅度、仓位、成本、风险和可执行性。方向预测只是很小一部分。

### 2. 为什么金融数据容易过拟合？

因为信噪比低、非平稳、样本数量看似多但有效独立样本少，并且研究者会反复试错。

### 3. 什么是未来函数？

在回测中使用了当时不可能知道的信息。例如用未来收益构造特征，用财报期末日而不是披露日，使用今天才知道的指数成分回测过去。

### 4. 什么是一个好因子？

不是单看某个指标高，而是逻辑合理、方向稳定、IC 稳定、分组单调、成本可控、容量足够、风险暴露清楚、样本外仍然有效。

### 5. 为什么要做风险归因？

因为策略赚钱不一定来自 alpha。归因能帮助判断收益来自模型能力，还是来自市场、行业、市值、风格或流动性风险。

---

## 14. 高频量化基础理解

高频量化不是把日频策略缩短到分钟级。它研究的是市场微观结构：订单如何进入市场、盘口如何变化、谁在主动成交、限价单能不能排到、成交后会不会被逆向选择。

中低频研究通常是：

```text
未来 5 天、20 天、60 天，哪些股票更可能跑赢？
```

高频研究通常是：

```text
未来几笔成交、几秒或几分钟，mid-price 会不会跳动？
我的订单能否成交？
成交价格相对参考价格损失多少？
这个信号是否足够覆盖半价差、手续费、滑点和冲击？
```

### 1. 高频里的价格不是一个价格，而是一组价格

日频里常看开高低收，高频里至少要区分：

- `Bid`：买一价。
- `Ask`：卖一价。
- `Mid = (Bid + Ask) / 2`：中间价。
- `Spread = Ask - Bid`：买卖价差。
- `MicroPrice`：按买卖盘挂单量加权后的微价格。

买入如果用市价单，通常要打到 `Ask`；卖出如果用市价单，通常要打到 `Bid`。所以高频策略哪怕方向判断正确，也可能被价差吃掉。

### 2. 高频数据三件套：盘口、逐笔委托、逐笔成交

盘口快照告诉你“当前挂了什么单”：

- 买一到买十价格和数量。
- 卖一到卖十价格和数量。
- 盘口深度、价差、队列状态。

逐笔委托告诉你“订单如何进入或退出市场”：

- 新增买单/卖单。
- 撤单。
- 改单。
- 委托方向、价格、数量、时间。

逐笔成交告诉你“真实成交发生了什么”：

- 成交价格。
- 成交数量。
- 成交时间。
- 主动买入或主动卖出。

只看逐笔成交会漏掉撤单信息；只看盘口快照会丢失事件顺序；只看日 K 线基本看不到高频 alpha。

### 3. 高频最常见的基础因子

订单簿不平衡：

```text
OBI = (BidSize_1 - AskSize_1) / (BidSize_1 + AskSize_1)
```

它衡量买一和卖一哪边压力更大。

微价格：

```text
MicroPrice = (Ask * BidSize_1 + Bid * AskSize_1)
             / (BidSize_1 + AskSize_1)
```

如果买盘很厚、卖盘很薄，价格更容易向上跳，微价格会高于中间价。

订单流不平衡：

```text
OFI = 买盘新增和卖盘减少带来的买压 - 卖盘新增和买盘减少带来的卖压
```

OFI 的核心是把新增委托、撤单、成交消耗和盘口价格变化统一成短期供需压力。

主动成交不平衡：

```text
TradeImbalance = AggressiveBuyVolume - AggressiveSellVolume
```

连续主动买入通常表示短期买压，但如果价格没有跟随上行，也可能说明上方卖压很强。

### 4. 高频建模的基本目标

高频模型不一定直接预测收益率，常见目标包括：

- 下一跳 mid-price 方向。
- 未来固定秒数的中间价收益。
- 未来固定事件数的价格变化。
- 买一/卖一队列是否耗尽。
- 限价单是否成交。
- 成交后是否发生逆向选择。
- 一笔母单的执行短缺。

高频标签要特别小心泄漏。例如用 `t` 时刻订单簿特征预测 `t+1` 事件，必须确认 `t+1` 的成交或撤单没有被提前混入特征。

### 5. 高频模型从强基线开始

推荐建模顺序：

```text
OBI / OFI / MicroPrice
-> 线性回归或 Logistic 回归
-> GBDT / LightGBM
-> Hawkes / queue-reactive 模型
-> CNN/LSTM/TCN/Transformer 订单簿模型
-> 强化学习执行或做市模型
```

复杂模型必须打败简单强基线才有意义。高频数据样本看似巨大，但独立信息少、制度约束强、交易成本敏感，深度模型很容易学到行情源细节或局部噪声。

### 6. 高频回测必须是事件驱动

日频回测可以粗略用下一日开盘、VWAP 或收盘价成交。高频回测如果还用理想价格，基本不可信。

高频回测至少要模拟：

- 订单提交时间。
- 行情延迟和策略计算延迟。
- 市价单吃盘口。
- 限价单排队位置。
- 部分成交。
- 撤单是否及时。
- 手续费和交易所费用。
- 价差、滑点、冲击成本。
- 涨跌停、停牌、集合竞价等交易制度。

关键指标不只是收益：

- fill ratio：挂单成交率。
- hit ratio：方向预测命中率。
- realized spread：成交后价差收益是否保住。
- adverse selection：成交后价格是否朝不利方向移动。
- implementation shortfall：实际成交相对决策时参考价的损失。
- latency sensitivity：延迟增加后收益衰减多快。

### 7. 高频策略的主要类型

短期方向预测：

- 根据订单簿、订单流和成交不平衡预测短期价格跳动。
- 难点是单次 alpha 很小，必须覆盖成本。

做市策略：

- 同时报买价和卖价，试图赚取价差。
- 难点是库存风险和逆向选择。

统计套利：

- 交易相关品种之间的短期偏离，例如 ETF 和成分股、期货和现货、可转债和正股。
- 难点是同步、冲击成本和交易制度。

执行算法：

- 不一定追求 alpha，而是把大订单以更低成本完成。
- 典型目标是最小化 implementation shortfall。

### 8. A 股高频要特别注意的边界

普通 A 股股票通常存在 T+1 约束，买入后当日不能卖出，所以“股票日内高频回转”天然受限。更适合高频或日内策略研究的品种通常包括：

- 股指期货、商品期货、国债期货。
- ETF 和部分可回转交易品种。
- 可转债。
- 期权。

A 股股票上的高频数据仍然有价值，但很多时候更适合用于：

- 优化建仓和减仓执行。
- 判断买入时点。
- 降低冲击成本。
- 预测短期流动性。
- 过滤不利盘口状态。

核心 insight：在有 T+1 约束的市场里，高频不一定用于频繁开平仓，也可以用于“更聪明地完成中低频策略给出的目标交易”。

### 9. 高频和中低频如何结合

一个更实战的结构是：

```text
日频/分钟级 alpha 决定买什么、卖什么、目标仓位是多少；
高频模型决定什么时候下单、用限价还是市价、拆多少、是否等待更好盘口。
```

也就是说：

- Alpha model：决定方向。
- Portfolio model：决定仓位。
- Execution model：决定成交方式。

高频模型不一定要创造独立 alpha，只要能稳定降低滑点和冲击，就能显著提高策略净收益。

---

## 15. 快速公式表

```text
简单收益率:
r_t = P_t / P_{t-1} - 1

组合收益:
r_p = sum(w_i * r_i)

年化收益:
ann_ret ≈ daily_mean * 252

年化波动:
ann_vol = daily_std * sqrt(252)

夏普:
Sharpe = ann_excess_return / ann_vol

最大回撤:
drawdown_t = NAV_t / historical_peak_t - 1

信息比率:
IR = ann_active_return / tracking_error

换手率:
turnover = sum(abs(w_t - w_{t-1})) / 2

净收益:
net_return = gross_return - trading_cost

因子 IC:
IC_t = corr(factor_t, future_return_t)

订单簿不平衡:
OBI = (BidSize_1 - AskSize_1) / (BidSize_1 + AskSize_1)

微价格:
MicroPrice = (Ask * BidSize_1 + Bid * AskSize_1) / (BidSize_1 + AskSize_1)

参与率:
participation = trade_value / market_volume

执行短缺:
IS = direction * (exec_price - decision_price) * quantity
```

---

## 16. 最重要的 insight 清单

- 量化的核心不是预测，而是把预测转化为可交易收益。
- 金融市场信噪比低，简单稳定的模型经常比复杂模型更可靠。
- 回测的第一职责是排错，不是证明自己正确。
- 因子收益要区分 alpha、beta、行业、风格和流动性风险。
- 成本、换手和容量会决定一个信号是否有商业价值。
- 用当时不可得的信息做回测，就是未来函数。
- 样本外表现比样本内表现重要。
- 分组单调性通常比单次多空收益更能说明因子质量。
- 组合优化的目标是把信号、风险、成本和约束放在一起权衡。
- 实盘表现偏离回测时，要从数据、信号、模型、组合、执行逐层排查。
- 高频量化的核心不是“交易次数多”，而是正确理解盘口、队列、成交概率、逆向选择和延迟。
- 在普通 A 股股票 T+1 约束下，高频数据更多用于优化执行、降低滑点和识别不利交易时点。
