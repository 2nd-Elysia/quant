﻿# 04. 组合构建

# 3. 从模型到组合：如何决定买多少？

现在进入下一层：**组合构建**。

如果模型预测了 (N) 只股票的未来收益：

$
\hat \mu_t =
\hat \mu_{t,1}, \hat \mu_{t,2}, \dots, \hat \mu_{t,N}^\top
$

你需要决定每只股票的权重：

$w_t =[w_{t,1}, w_{t,2}, \dots, w_{t,N}]^\top$组合的预测收益是：

$ \hat r_{p,t}=w_t^\top \hat \mu_t
$

组合风险可以用协方差矩阵表示。

------

# 3.1 协方差矩阵：资产之间不是独立的

假设有 (N) 只股票，它们的收益不是独立的。

比如：

- 银行股之间通常一起涨跌。
- 半导体股之间通常一起涨跌。
- 市场大跌时，大多数股票一起跌。

所以要描述组合风险，需要协方差矩阵：

$ \Sigma=\begin{bmatrix}
\sigma_{11} & \sigma_{12} & \cdots & \sigma_{1N} \\
\sigma_{21} & \sigma_{22} & \cdots & \sigma_{2N} \\
\vdots & \vdots & \ddots & \vdots \\
\sigma_{N1} & \sigma_{N2} & \cdots & \sigma_{NN}
\end{bmatrix}
$

其中：

- (\sigma_{ii})：第 (i) 只股票自身收益方差。
- (\sigma_{ij})：第 (i) 只和第 (j) 只股票收益的协方差。

组合方差是：

$
\sigma_p^2 = w^\top \Sigma w
$

组合波动率是：

$
\sigma_p = \sqrt{w^\top \Sigma w}
$

这就是为什么分散投资有用。

如果股票之间相关性不高，组合整体风险可能低于单个股票风险。

------

# 3.2 最经典的组合优化：均值-方差模型

组合优化的经典形式是：

$\max_w ; w^\top \hat\mu - \frac{\lambda}{2} w^\top \Sigma w$

这句话非常重要。

它的含义是：

> 在追求预测收益的同时，惩罚组合风险。

展开解释：

- (w)：你要优化的股票权重。
- (\hat\mu)：模型预测收益。
- (w^\top \hat\mu)：组合预期收益。
- (\Sigma)：收益协方差矩阵。
- $(w^\top \Sigma w)$：组合风险。
- (\lambda)：风险厌恶系数。

如果 (\lambda) 很大，策略会更保守，更重视风险控制。

如果 (\lambda) 很小，策略会更激进，更重视预测收益。

------

# 3.3 加上真实约束后，才像实际量化策略

上面的优化太理想化。实际策略会加很多约束。

## 权重和为 1

$
\sum_{i=1}^{N} w_i = 1
$

表示资金全部分配出去。

------

## 只能做多

$
w_i \geq 0
$

做多就是买入股票，希望它上涨赚钱。

如果允许做空，则 (w_i) 可以小于 0。

------

## 单只股票不能买太多

$
0 \leq w_i \leq w_{\max}
$

比如每只股票最多占组合 5%。

这是为了避免单票风险。

------

## 行业暴露约束

假设 (A_{industry}) 是行业暴露矩阵，组合行业暴露是：

$
A_{industry}^\top w
$

可以约束为接近基准指数：

$
|A_{industry}^\top w - A_{industry}^\top w^{bench}| \leq c
$

这里的 (w^{bench}) 是基准指数权重，比如沪深 300、中证 500、中证 1000。

通俗解释：

> 如果你的基准里银行占 10%，你不能让策略银行占 50%，否则你的收益很大程度来自行业赌注。

------

## 换手率约束

上一期权重是：

$
w_{t-1}
$

这期权重是：

$
w_t
$

换手可以近似写为：

$
Turnover_t = \sum_{i=1}^{N} |w_{t,i} - w_{t-1,i}|
$

换手越高，交易越频繁，成本越高。

所以可以约束：

$
\sum_{i=1}^{N} |w_{t,i} - w_{t-1,i}| \leq \tau
$

这表示本次调仓不能变动太大。

------

# 3.4 加入交易成本后的优化

如果考虑交易成本，目标函数可以写成：

$ \max_w \quad w^\top \hat\mu-\frac{\lambda}{2} w^\top \Sigma w-Cost(w, w_{old})
$

交易成本通常和权重变化有关：

$ Cost(w, w_{old})=\sum_{i=1}^{N} c_i |w_i - w_{old,i}|
$

其中：

- (w_{old})：当前持仓。
- (w)：目标持仓。
- (c_i)：交易第 (i) 只股票的单位成本。

这说明：

> 模型预测收益必须足够大，才值得为了它去调仓。

如果某只股票预测收益只比原来高一点点，但交易成本很大，那可能不值得换。

------

# 3.5 基准指数是什么？

很多量化策略不是绝对地追求赚钱，而是追求跑赢某个基准。

比如：

- 沪深 300：代表 A 股大盘蓝筹。
- 中证 500：代表中盘股票。
- 中证 1000：代表更小市值股票。
- 标普 500：代表美国大盘股票。

基准指数可以理解成：

> 一个市场代表性组合。

如果你的策略以沪深 300 为基准，目标不是简单赚钱，而是：

$
r_p - r_{bench}
$

也就是策略收益减去基准收益。

这个叫**超额收益**。

$
r_{excess} = r_p - r_{bench}
$

量化选股里，经常追求的是稳定的超额收益，而不是绝对收益。

因为如果市场整体涨 30%，你赚 20%，其实你跑输了。

如果市场跌 20%，你跌 5%，虽然绝对亏钱，但相对基准表现很好。

------

# 3.6 Alpha 和 Beta：量化里最核心的金融概念之一

我们经常说量化研究要找 alpha。

先看一个简单模型：

$
r_i = \alpha_i + \beta_i r_m + \epsilon_i
$

其中：

- (r_i)：股票或策略收益。
- (r_m)：市场收益。
- (\beta_i)：对市场的敏感度。
- (\alpha_i)：不能被市场涨跌解释的超额收益。
- (\epsilon_i)：噪声。

如果：

$
\beta_i = 1
$

说明市场涨 1%，它大概也涨 1%。

如果：

$
\beta_i = 1.5
$

说明它比市场更激进，市场涨 1%，它可能涨 1.5%；市场跌 1%，它可能跌 1.5%。

如果：

$
\beta_i = 0.5
$

说明它比市场更稳，市场涨跌对它影响较小。

量化研究真正想要的是：

$
\alpha_i > 0
$

也就是：

> 在扣除市场整体影响之后，仍然有额外收益。

------

# 3.7 主动收益和主动风险

如果策略对标一个基准，组合权重是 (w)，基准权重是 (w^b)。

主动权重是：

$
h = w - w^b
$

策略相对基准的超额收益是：

$
r_p - r_b = h^\top r
$

主动风险可以写成：

$
\sigma_{active} = \sqrt{h^\top \Sigma h}
$

这表示你的组合偏离基准所带来的风险。

很多机构量化策略不是让 (w) 随便优化，而是优化主动权重 (h)。

也就是：

> 在不要过度偏离基准的情况下，尽量获得超额收益。

------

# 3.8 信息比率 IR：衡量超额收益质量

如果夏普比率衡量的是绝对收益相对总风险，那么信息比率 IR 衡量的是：

> 超额收益相对主动风险有多高。

定义：

$
IR =
\frac{\mathbb{E}[r_p - r_b]}
{\sigma(r_p - r_b)}
$

其中：

- (r_p)：策略收益。
- (r_b)：基准收益。
- (r_p-r_b)：超额收益。
- (\sigma(r_p-r_b))：超额收益波动。

对于指数增强策略，IR 是非常重要的指标。

指数增强就是：

> 大体跟住某个指数，但通过量化选股争取每年多赚一点。

例如目标是：

$
\text{沪深300收益} + 5%
$

这就是典型的量化增强思路。

------

# 3.9 一个完整例子：从模型预测到持仓

假设现在只有 5 只股票，模型预测收益如下：

| 股票 | 预测未来收益 |
| ---- | ------------ |
| A    | 1.2%         |
| B    | 0.8%         |
| C    | 0.2%         |
| D    | -0.3%        |
| E    | -0.6%        |

最简单的策略可能是：

- 买 A、B。
- 不买 D、E。

但实际还要考虑：

| 股票 | 预测收益 | 风险 | 流动性 | 行业   |
| ---- | -------- | ---- | ------ | ------ |
| A    | 1.2%     | 高   | 差     | 半导体 |
| B    | 0.8%     | 中   | 好     | 银行   |
| C    | 0.2%     | 低   | 好     | 医药   |
| D    | -0.3%    | 中   | 好     | 半导体 |
| E    | -0.6%    | 高   | 差     | 传媒   |

如果 A 虽然预测收益最高，但风险高、流动性差、交易成本大，最终不一定买最多。

组合优化可能给出：

| 股票 | 最终权重 |
| ---- | -------- |
| A    | 15%      |
| B    | 35%      |
| C    | 30%      |
| D    | 15%      |
| E    | 5%       |

你会发现，预测收益最低的 E 也可能有一点权重。

原因可能是：

- 它帮助降低组合和基准的偏离。
- 它属于某个需要保留的行业。
- 它和其他股票相关性较低。
- 它是原来持仓，卖掉成本太高。

这说明：

> 预测模型负责判断相对吸引力，组合优化负责在真实约束下分配资金。

------

# 3.10 本节小结

这一节的主线是：

$
\text{原始信号}
\rightarrow
\text{标准化/去极值/中性化}
\rightarrow
\text{预测模型}
\rightarrow
\text{预期收益}
\rightarrow
\text{组合权重}
$

用数学写就是：

$
x_{t,i}
\rightarrow
z_{t,i}
\rightarrow
\hat\mu_{t,i}
\rightarrow
w_{t,i}
$

其中：

- (x_{t,i})：原始因子。
- (z_{t,i})：处理后的因子。
- (\hat\mu_{t,i})：模型预测收益。
- (w_{t,i})：最终持仓权重。

量化研究的关键不是单独某个公式，而是这套逻辑：

> 信号必须先被清洗、标准化、中性化；
> 模型预测的是相对收益或排序；
> 最终交易的是组合权重；
> 权重必须服从风险、成本、行业、流动性等约束。

------

# 3.11 前沿组合构建：从均值方差到成本感知优化

经典均值方差模型假设预测收益和协方差都比较可靠，但真实量化里这两个量都很不稳定。更实用的组合优化会把预测不确定性、成本、约束和容量一起放进去。

## 1. Alpha 强度要转成可交易收益

模型输出常常只是分数：

```text
score_i
```

组合优化需要的是预期收益：

```text
mu_i
```

常见转换方式：

- 用历史分组收益把分数映射成收益。
- 用横截面回归估计因子收益。
- 用 `IC * volatility * score` 做粗略缩放。
- 按行业或市值组分别校准。
- 对预测收益做 shrinkage，避免过度相信模型。

实务中不能让一个未经校准的模型分数直接进入优化器，否则优化器会把微小分数差异放大成极端仓位。

## 2. 鲁棒优化

如果预期收益估计误差很大，最优组合会非常不稳定。鲁棒优化的思想是：

```text
不要只优化最乐观的 mu，而要考虑 mu 估计错了以后组合是否仍可接受。
```

常见做法：

- 对 `mu` 做 shrinkage。
- 限制单票权重和行业偏离。
- 限制主动风险。
- 对协方差矩阵做 shrinkage 或使用因子风险模型。
- 加入 turnover penalty，减少仓位跳动。

## 3. 成本感知组合优化

更贴近实盘的目标函数：

```text
max_w w' mu - lambda / 2 * w' Sigma w - Cost(w, w_old)
```

成本不应只用一个固定费率。可以拆成：

```text
Cost = 手续费 + 税费 + spread cost + impact cost + borrow cost
```

冲击成本通常和参与率、波动率、成交额有关：

```text
Impact_i ~= k * sigma_i * (trade_value_i / ADV_i)^alpha * trade_value_i
```

这会自然降低小盘、低流动性、高换手股票的目标仓位。

## 4. No-trade region

因为交易有成本，模型分数小幅变化时不一定要交易。

```text
如果 |target_weight - current_weight| < threshold，则不交易
```

更高级的做法是让阈值随股票成本变化：

```text
成本越高、流动性越差，no-trade region 越宽。
alpha 越强、风险越低，越允许调仓。
```

## 5. 风险预算和主动风险

指数增强或相对收益策略更关心主动权重：

```text
h = w - w_benchmark
```

优化目标可以写成：

```text
max_h  h' alpha - lambda / 2 * h' Sigma h - Cost(h, h_old)
```

并约束：

- 行业主动暴露。
- 风格主动暴露。
- 跟踪误差。
- 单票主动权重。
- 组合换手。
- 单票成交占 ADV 比例。

核心是：只在你认为有 alpha 的地方承担主动风险。

## 6. 高频组合和做市里的“仓位”是库存

高频做市或执行策略里，组合权重不是最核心变量，库存更重要：

```text
inventory_t = 当前持仓数量或风险暴露
```

做市模型要同时决定：

- bid 报价离 mid 多远。
- ask 报价离 mid 多远。
- 每边挂多少量。
- 当前库存过多时如何倾斜报价。
- 什么时候撤单。

典型目标：

```text
max E[spread capture - adverse selection - inventory risk - fees]
```

如果库存太多，价格反向波动会让做市策略亏损，所以做市不是简单“同时挂买卖赚价差”，而是在价差收益、成交概率、逆向选择和库存风险之间权衡。

## 7. 高频执行里的组合决策

如果中低频模型给出目标交易量 `Q`，高频执行模型要决定：

```text
q_1, q_2, ..., q_K
```

即每个时间片或事件状态下交易多少。

常见约束：

- 总交易量必须完成。
- 每段参与率不能太高。
- 不能在盘口极差时强行交易。
- 价格风险不能太大。
- 延迟和撤单失败要纳入压力测试。

这类问题可以用 Almgren-Chriss 框架、动态规划、模型预测控制或强化学习处理。

## 8. 组合构建检查清单

- 预测收益是否经过校准，而不是直接使用模型分数？
- 优化器是否因为微小 alpha 差异产生极端仓位？
- 协方差矩阵是否稳定？
- 成本是否随股票流动性和交易规模变化？
- 换手约束是否会显著牺牲 alpha？
- 行业、风格、市值、流动性暴露是否符合策略目标？
- 容量扩大后净收益是否还能接受？
- 高频执行是否考虑库存、排队、成交概率和逆向选择？

