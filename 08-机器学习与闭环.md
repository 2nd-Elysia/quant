# 08. 机器学习与闭环

# 7. 机器学习在量化研究里做什么？

一句话：

> **机器学习在量化里主要用于把多个信号 (x) 合成为一个对未来收益的预测 ($\hat{y}$)，但最终目标不是预测准确率，而是可交易的风险调整收益。**

------

## 7.1 量化里的监督学习形式

最基本的数据形式是：

$
(t,i) \rightarrow (x_{t,i}, y_{t,i})
$

其中：

- (t)：时间。
- (i)：股票。
- (x_{t,i})：第 (t) 天能看到的特征。
- (y_{t,i})：未来收益标签。

例如：

$
y_{t,i} = \frac{P_{t+H,i}}{P_{t,i}} - 1
$

表示第 (i) 只股票未来 (H) 天收益率。

机器学习模型学习的是：

$
\hat{y}*{t,i} = f(x*{t,i})
$

也就是：

> 用今天能看到的信息，预测未来一段时间的收益或相对排名。

------

## 7.2 标签怎么构造？

常见标签有几类。

### 1. 未来收益率

$
y_{t,i} = \frac{P_{t+H,i}}{P_{t,i}} - 1
$

例如未来 5 日收益、未来 20 日收益。

这是最直接的标签。

------

### 2. 超额收益

如果有基准 (b)，可以用：

$
y_{t,i}^{excess} = r_{t+H,i} - r_{t+H,b}
$

表示股票相对基准的收益。

这更适合指数增强或相对收益策略。

------

### 3. 排名标签

很多时候我们不关心收益具体是多少，而关心排序：

$
rank(y_{t,i})
$

模型只需要知道哪些股票未来相对更好。

所以量化里经常更关注：

$
RankIC = Corr(rank(\hat{y}), rank(y))
$

而不是普通回归误差。

------

## 7.3 为什么不能随机切分训练集和测试集？

普通机器学习里常见做法是随机划分：

$
Train / Validation / Test
$

但量化里不能随便随机切，因为金融数据有时间顺序。

正确方式通常是：

$
\text{过去训练} \rightarrow \text{未来验证} \rightarrow \text{更未来测试}
$

例如：

| 时间段    | 用途 |
| --------- | ---- |
| 2016–2020 | 训练 |
| 2021      | 验证 |
| 2022–2024 | 测试 |

因为实盘中你只能用过去预测未来。

如果随机打乱，就可能出现：

> 模型在训练时看到了未来市场状态的信息。

这会造成时间泄漏。

------

## 7.4 常用模型有哪些？

### 1. 线性模型

$
\hat{y}*{t,i} = \beta^\top x*{t,i}
$

优点：

- 简单。
- 稳定。
- 可解释。
- 不容易过拟合。

在金融这种低信噪比场景下，线性模型仍然很重要。

------

### 2. Ridge / Lasso

Ridge：

$
\min_\beta \sum_j (y_j - x_j^\top \beta)^2 + \lambda |\beta|_2^2
$

Lasso：

$
\min_\beta \sum_j (y_j - x_j^\top \beta)^2 + \lambda |\beta|_1
$

它们主要用来抑制过拟合。

------

### 3. 树模型

比如 GBDT、XGBoost、LightGBM。

它们适合捕捉非线性关系，例如：

> 低估值因子只在高质量公司中有效。

这种交互关系线性模型不容易表达，树模型更擅长。

------

### 4. 深度学习

常用于：

- 高频订单簿建模。
- 新闻文本情绪分析。
- 财报文本建模。
- 多资产时间序列。
- 多模态另类数据。

但在普通中低频选股里，深度学习不一定稳定优于简单模型。原因是金融数据噪声大、非平稳、有效样本少，而且实盘更关心成本和稳定性。

------

## 7.5 量化 ML 和普通 ML 最大区别

普通机器学习经常关心：

$
MSE,\ Accuracy,\ AUC
$

但量化更关心：

$
IC,\ RankIC,\ Sharpe,\ IR,\ Drawdown,\ Turnover,\ Cost
$

也就是说，模型预测准不准只是中间指标。

最终要看：

$
\hat{y}
\rightarrow
w
\rightarrow
R_{net}
$

也就是预测能不能转成仓位，仓位能不能转成扣成本后的收益。

所以一个模型即使 MSE 很低，也可能不赚钱。

反过来，一个模型点预测误差不小，但排序能力稳定，也可能有交易价值。

------

## 7.6 量化 ML 的关键风险

主要有四个。

第一，**未来函数**：

$
x_t \notin \mathcal{F}_t
$

如果特征里用了未来才知道的信息，模型会虚假有效。

第二，**过拟合**：

金融信噪比很低，复杂模型容易记住历史噪声。

第三，**非平稳性**：

$
P_{train}(x,y) \neq P_{test}(x,y)
$

市场结构会变化，历史规律可能失效。

第四，**预测目标和交易目标不一致**：

训练时优化：

$
\min MSE
$

但实盘想最大化：

$
\max Sharpe \quad \text{or} \quad \max IR
$

这两个目标并不完全一致。

------

# 8. 回头检查：最开始的流程是否跑通？

最开始我们说，量化研究主线是：

$
\text{市场数据}
\rightarrow
\text{假设}
\rightarrow
\text{信号/因子}
\rightarrow
\text{模型}
\rightarrow
\text{策略}
\rightarrow
\text{回测}
\rightarrow
\text{风控}
\rightarrow
\text{实盘验证}
$

现在看，这条链路基本已经跑通了。

------

## 8.1 市场数据

我们讲了价格、收益率、成交量、财务数据、行业、市值、订单簿等。

核心对象是：

$
P_{t,i},\quad r_{t,i},\quad x_{t,i}
$

其中价格本身不是重点，重点是收益率和可预测特征。

------

## 8.2 投资假设

我们讲了动量、反转、价值、质量等假设。

核心问题是：

$
x_{t,i} \quad \text{是否能预测} \quad y_{t,i}
$

比如：

$
\mathbb{E}[y \mid x \text{高}]\gt\mathbb{E}[y \mid x \text{低}$
$

------

## 8.3 信号 / 因子

我们讲了因子的构造、标准化、去极值、中性化。

主线是：

$
x_{t,i}
\rightarrow
z_{t,i}
$

其中 (z_{t,i}) 是处理后的可用信号。

------

## 8.4 模型

我们讲了单因子模型、多因子模型、线性模型、树模型、深度学习。

主线是：

$
z_{t,i}
\rightarrow
\hat{\mu}_{t,i}
$

也就是从因子得到预测收益或股票评分。

------

## 8.5 组合构建

我们讲了如何从预测收益变成仓位。

核心是组合优化：

$ \max_w \quad w^\top \hat{\mu}-\frac{\lambda}{2}w^\top \Sigma w-Cost(w,w_{old})
$

输出是：

$
w_t
$

也就是每只股票买多少。

------

## 8.6 回测验证

我们讲了净值、年化收益、波动率、夏普、最大回撤、信息比率、换手率、交易成本、未来函数、幸存者偏差、过拟合。

核心是：

$
R_{p,t+1} = w_t^\top r_{t+1}
$

以及：

$
NAV_t = NAV_{t-1}(1+R_{net,t})
$

------

## 8.7 风险模型与归因

我们讲了 alpha、beta、行业暴露、风格暴露、特异风险、主动权重、主动风险。

核心是：

$
r_{i,t}=\alpha_i+\sum_{k=1}^{K}\beta_{i,k}f_{k,t}+\epsilon_{i,t}
$

以及组合暴露：

$
b_p = B^\top w
$

这一步回答：

> 策略到底赚的是 alpha，还是赚了市场、行业、市值、风格的钱？

------

## 8.8 交易执行

我们讲了目标权重、交易金额、买卖价差、滑点、市场冲击、VWAP、TWAP、容量。

核心是：

$
w_t^{target}
\rightarrow
\Delta V_t
\rightarrow
\text{成交}
\rightarrow
w_t^{real}
$

以及：

$ R_{net,t}=R_{gross,t}-\frac{Cost_t}{A_t}
$

这一步回答：

> 纸面策略能不能真实交易出来？

------

# 9. 唯一还需要补一句：实盘监控与迭代

前面的流程已经完整，但还可以补上最后一环：**实盘监控**。

策略上线后，研究员还要持续观察：

$
R_{live} \quad \text{是否接近} \quad R_{backtest}
$

重点看：

- 实盘收益是否显著低于回测。
- 滑点是否变大。
- 因子 IC 是否衰减。
- 风险暴露是否偏离。
- 换手和成本是否异常。
- 市场环境是否变化。
- 策略是否变得拥挤。

如果实盘表现偏离预期，就要重新检查：

$
\text{数据} \rightarrow \text{信号} \rightarrow \text{模型} \rightarrow \text{组合} \rightarrow \text{执行}
$

这形成一个闭环：

$
\text{研究}
\rightarrow
\text{回测}
\rightarrow
\text{实盘}
\rightarrow
\text{监控}
\rightarrow
\text{再研究}
$

------

# 10. 最终总结

量化研究本质上是在做一件事：

> **用数据和模型寻找可重复的投资规律，并在风险、成本、容量、交易约束下，把它变成可执行的投资策略。**

完整流程可以压缩成这条链：

$
\boxed{
Data
\rightarrow
Signal
\rightarrow
Model
\rightarrow
Portfolio
\rightarrow
Backtest
\rightarrow
Risk
\rightarrow
Execution
\rightarrow
Live
}
$

对应中文就是：

$
\boxed{
数据
\rightarrow
信号
\rightarrow
模型
\rightarrow
组合
\rightarrow
回测
\rightarrow
风控归因
\rightarrow
交易执行
\rightarrow
实盘迭代
}
$

你可以把量化研究理解为三个层次：

**第一层：预测。**

$
x_{t,i} \rightarrow \hat{\mu}_{t,i}
$

找什么信息能预测未来收益。

**第二层：决策。**

$
\hat{\mu}_t \rightarrow w_t
$

在风险和约束下决定买什么、买多少。

**第三层：落地。**

$
w_t \rightarrow R_{net}
$

通过回测、风控、交易执行和实盘监控，判断策略是否真的能赚钱。

最关键的认知是：

> 量化研究不是简单地“用机器学习预测涨跌”，而是一个从数据、统计规律、组合优化、风险控制到交易执行的完整系统工程。
