﻿﻿# 03. 因子处理与预测模型


我们继续讲第二大块：

# 2. 从信号到模型：如何把 (x_{t,i}) 变成预测值？

上一节我们讲的是：

$
x_{t,i} \rightarrow y_{t,i}
$

其中：

- (x_{t,i})：第 (t) 天，第 (i) 只股票的信号。
- (y_{t,i})：未来收益率，比如未来 5 天收益。
- 研究目标：判断 (x) 是否能预测 (y)。

但真实量化研究不会只停留在“这个因子有没有用”，还要进一步问：

> 如果有很多个信号，怎么综合成一个最终预测？
> 如果模型给出预测，怎么决定买多少？
> 如果有风险和交易成本，怎么修正仓位？

这一节先讲：**从单个信号到多因子模型。**

------

# 2.1 为什么不能直接用原始信号？

假设你有三个信号：

| 信号 | 含义           | 数值尺度         |
| ---- | -------------- | ---------------- |
| 动量 | 过去 20 日涨幅 | (-20%) 到 (+30%) |
| 市值 | 公司总市值     | 几十亿到几万亿   |
| ROE  | 净资产收益率   | (-50%) 到 (+50%) |

它们的单位和尺度完全不同。

如果直接扔进模型，尺度大的变量可能主导结果。比如市值动辄上百亿，而动量只有 0.1 左右。

所以量化研究里，第一步通常是对信号做**标准化**。

------

# 2.2 截面标准化：把不同股票放在同一天比较

量化股票研究经常做的是**截面比较**。

也就是说，固定某一天 (t)，比较所有股票的特征。

假设某个因子是：

$
x_{t,i}
$

在第 (t) 天，所有股票的平均值是：

$
\mu_t = \frac{1}{N}\sum_{i=1}^{N} x_{t,i}
$

标准差是：

$
\sigma_t = \sqrt{\frac{1}{N-1}\sum_{i=1}^{N}(x_{t,i}-\mu_t)^2}
$

标准化后的信号是：

$
z_{t,i} = \frac{x_{t,i}-\mu_t}{\sigma_t}
$

这个 (z_{t,i}) 就是常说的 **z-score**。

它的含义是：

> 第 (i) 只股票在当天相对全市场平均水平高了几个标准差。

例如：

$
z_{t,i} = 2
$

表示这只股票的该因子值比当天平均水平高 2 个标准差。

$
z_{t,i} = -1.5
$

表示它比当天平均水平低 1.5 个标准差。

------

# 2.3 为什么按“当天截面”标准化，而不是全历史标准化？

因为股票量化经常关心的是：

> 今天这只股票相对其他股票怎么样？

而不是：

> 这只股票今天相对自己历史怎么样？

这两种都可以做，但含义不同。

## 截面标准化

$
z_{t,i}^{cross} = \frac{x_{t,i} - \mu_t^{cross}}{\sigma_t^{cross}}
$

比较的是同一天不同股票之间的差异。

适合回答：

> 今天应该买哪几只股票？

------

## 时间序列标准化

$
z_{t,i}^{time} = \frac{x_{t,i} - \mu_i^{time}}{\sigma_i^{time}}
$

比较的是同一只股票现在相对过去是否异常。

适合回答：

> 这只股票现在是否处于自身历史极端状态？

股票多因子选股里，截面标准化更常见。

------

# 2.4 极值处理：为什么要 winsorize？

金融数据里经常有异常值。

例如某只股票因为财报异常、停牌、重组、数据错误，导致某个因子特别大。

如果不处理，它可能严重影响模型。

常见做法是把极端值截断，叫 **winsorization**。

例如把标准化后的因子限制在 ($-3,3])：

$
z_{t,i}^{clip} = \max(-3, \min(z_{t,i}, 3))
$

也就是说：

- 大于 3 的，压到 3。
- 小于 -3 的，压到 -3。
- 中间的保持不变。

这不是为了“篡改数据”，而是为了防止极端噪声支配模型。

------

# 2.5 行业中性化：避免信号只是行业差异

这是量化里非常重要的金融常识。

假设你发现银行股的 PE 普遍低，科技股的 PE 普遍高。

如果你直接用低 PE 选股，模型可能会买很多银行股，卖掉很多科技股。

这时候策略看起来是“价值因子”，但实际上可能只是“重仓银行、低配科技”。

为了避免这个问题，量化研究经常做**行业中性化**。

------

## 什么是行业？

股票市场会把公司分到不同产业类别：

- 银行
- 食品饮料
- 医药
- 半导体
- 计算机
- 新能源
- 房地产
- 传媒

行业本身会影响股票表现。

例如利率上升可能影响银行，政策变化可能影响医药，景气周期可能影响半导体。

所以我们不希望一个因子只是偷偷表达“买某个行业”。

------

## 行业中性化的数学形式

假设原始因子是：

$
x_{t,i}
$

我们用行业哑变量解释它：

$ x_{t,i}=\beta_0
+
\beta_1 I_{i,1}
+
\beta_2 I_{i,2}
+
\cdots
+
\beta_K I_{i,K}
+
\epsilon_{t,i}
$

其中：

- (I_{i,k})：如果股票 (i) 属于行业 (k)，则为 1，否则为 0。
- (K)：行业数量。
- (\epsilon_{t,i})：无法被行业解释的剩余部分。

然后我们用残差作为新的因子：
$$
x_{t,i}^{neutral} = \epsilon_{t,i}
$$
这表示：

> 在剔除行业差异之后，这只股票的因子值有多高。

通俗说，行业中性化之后，低 PE 的银行不会天然比高 PE 的科技股占便宜；它们更多是在各自行业内部比较。

------

# 2.6 市值中性化：避免信号只是买小盘股或大盘股

另一个常见问题是市值暴露。

市值是：

$
\text{Market Cap}_i = P_i \times \text{Shares}_i
$

也就是：

> 股票价格 × 总股本。

市值大的公司叫大盘股，市值小的公司叫小盘股。

小盘股和大盘股的收益特征经常不同。很多因子如果不处理，最后可能变成“小盘股策略”。

所以可以做市值中性化：

$ x_{t,i}=\beta_0
+
\beta_1 \log(\text{MarketCap}_{t,i})
+
\epsilon_{t,i}
$

取残差：

$
x_{t,i}^{neutral} = \epsilon_{t,i}
$

如果同时控制行业和市值，可以写成：

$ x_{t,i}=\beta_0
+
\sum_{k=1}^{K}\beta_k I_{i,k}
+
\gamma \log(\text{MarketCap}*{t,i})
+
\epsilon*{t,i}
$

最终使用：

$
x_{t,i}^{neutral} = \epsilon_{t,i}
$

这一步的目的不是让因子“更好看”，而是确认你真正研究的是该因子本身，而不是某些隐藏暴露。

------

# 2.7 单因子模型：最简单的预测形式

假设你只有一个信号，比如动量因子：

$
z_{t,i}^{mom}
$

你可以用一个线性模型预测未来收益：

$ \hat y_{t,i}=\alpha
+
\beta z_{t,i}^{mom}
$

其中：

- (\hat y_{t,i})：模型预测的未来收益。
- (\alpha)：截距。
- (\beta)：因子权重。
- (z_{t,i}^{mom})：标准化后的动量信号。

如果：

$
\beta > 0
$

表示动量越高，预测收益越高。

如果：

$
\beta < 0
$

表示动量越高，预测收益越低，这更像反转。

------

# 2.8 多因子模型：把多个信号加起来

真实量化研究中，通常不会只用一个因子，而是用多个因子。（上千因子）

例如：

- 动量因子
- 反转因子
- 价值因子
- 质量因子
- 成长因子
- 波动率因子
- 流动性因子
- 情绪因子

可以写成：

$ \hat y_{t,i}=\alpha
+
\beta_1 z_{t,i}^{mom}
+
\beta_2 z_{t,i}^{value}
+
\beta_3 z_{t,i}^{quality}
+
\cdots
+
\beta_K z_{t,i}^{K}
$

更紧凑地写：

$ \hat y_{t,i}=\alpha
+
\beta^\top z_{t,i}
$

其中：

$ z_{t,i}=[z_{t,i}^{1}, z_{t,i}^{2}, \dots, z_{t,i}^{K}]^\top$

$
 \beta=[\beta_1, \beta_2, \dots, \beta_K]^\top
$

用机器学习语言说，这就是一个线性回归模型。

区别在于，金融场景下你不能只看训练误差，还要看：

- 样本外 IC。
- 分组收益。
- 换手率。
- 交易成本。
- 风险暴露。
- 回撤。
- 实盘稳定性。

------

# 2.9 线性模型为什么在量化里仍然很重要？

你可能会问：现在都深度学习了，为什么还讲线性模型？

因为线性模型有几个优点：

第一，可解释。

$
\beta_k > 0
$

表示第 (k) 个因子对预测收益是正贡献。

第二，不容易过拟合。

金融数据噪声非常大，复杂模型很容易学到历史噪声。

第三，便于风险控制。

线性模型的输出更容易分解和解释。

第四，量化投资中很多收益来自“排序”，不是精确预测。

很多时候我们不需要准确预测某只股票未来涨 1.23%，只需要知道：

> 哪些股票相对更值得买，哪些相对不值得买。

所以量化模型的核心经常是排序问题，而不是传统意义上的精确回归。

------

# 2.10 排序比预测数值更重要

假设模型预测：

| 股票 | 预测收益 |
| ---- | -------- |
| A    | 0.8%     |
| B    | 0.6%     |
| C    | 0.1%     |
| D    | -0.2%    |
| E    | -0.5%    |

量化策略可能并不关心 A 到底是不是真的涨 0.8%。

它更关心：

$
A > B > C > D > E
$

也就是 A、B 更值得买，D、E 更应该少买或卖出。

所以很多股票量化模型评估的是 rank IC：

$ RankIC_t=Corr(rank(x_{t,\cdot}), rank(y_{t,\cdot}))
$

RankIC 用的是排序相关性，比普通线性相关对极端值更稳健。

如果一个模型的 RankIC 长期稳定为正，说明它能较稳定地区分“未来相对表现更好”和“未来相对表现更差”的股票。

------

# 2.11 因子合成：不用训练模型也可以做

有时候研究员不会直接训练复杂模型，而是先对多个因子加权合成。

例如：

$ s_{t,i}=w_1 z_{t,i}^{mom}
+
w_2 z_{t,i}^{value}
+
w_3 z_{t,i}^{quality}
$

这里 (s_{t,i}) 是综合得分。

权重可以来自：

1. 等权重。
2. 历史 IC 大小。
3. IC 稳定性。
4. 回归系数。
5. 机器学习模型输出的重要性。
6. 主观研究判断。

最简单的等权方式是：

$ s_{t,i}=\frac{1}{K}
\sum_{k=1}^{K} z_{t,i}^{k}
$

如果 (K) 个因子方向都处理好了，综合得分越高，说明股票越值得买。

------

# 2.12 因子方向：必须统一

这个细节很重要。

有些因子越高越好，比如：

$
ROE \uparrow \Rightarrow \text{可能更好}
$

有些因子越低越好，比如：

$
PE \downarrow \Rightarrow \text{可能更便宜}
$

如果你直接把它们加起来，方向会乱。

所以要统一成：

> 因子值越高，预测收益越高。

例如 PE 越低越好，可以改成：

$
EP = \frac{1}{PE}
$

或者：

$
x^{value} = -PE
$

这样高分都代表更有吸引力。

------

# 2.13 模型训练时的数据结构

股票量化的样本通常长这样：

$
(t,i) \mapsto (x_{t,i}, y_{t,i})
$

也就是每一个样本由时间和股票共同决定。

可以理解为表格：

| 日期       | 股票 | 动量 | 价值 | 质量 | 未来收益 |
| ---------- | ---- | ---- | ---- | ---- | -------- |
| 2020-01-01 | A    | 0.5  | 1.2  | 0.3  | 0.4%     |
| 2020-01-01 | B    | -0.3 | 0.8  | 0.9  | -0.2%    |
| 2020-01-01 | C    | 1.1  | -0.5 | 0.2  | 0.7%     |
| 2020-01-02 | A    | 0.4  | 1.1  | 0.3  | 0.1%     |
| 2020-01-02 | B    | -0.2 | 0.7  | 1.0  | 0.3%     |

机器学习视角：

$
X \in \mathbb{R}^{M \times K}
$

$
y \in \mathbb{R}^{M}
$

其中：

- (M)：样本数量，等于“日期 × 股票”的数量。
- (K)：因子数量。
- (X)：特征矩阵。
- (y)：未来收益标签。

但是注意，不能随机打乱训练集和测试集。

因为金融数据有时间顺序。

正确方式通常是：

$
\text{过去训练} \rightarrow \text{未来测试}
$

例如：

- 2015–2020 训练。
- 2021 验证。
- 2022–2024 测试。

不能把 2024 年的数据随机混进训练集，再去预测 2021 年。

那会产生时间泄漏。

------

# 2.14 用机器学习模型做量化预测

线性模型只是起点，实际也会用很多机器学习方法。

## 线性回归

$
\hat y = X\beta
$

优点是简单、稳定、可解释。

------

## Ridge / Lasso

Ridge：

$
\min_{\beta}
\sum_{j=1}^{M}(y_j - x_j^\top \beta)^2
+
\lambda |\beta|_2^2
$

Lasso：

$
\min_{\beta}
\sum_{j=1}^{M}(y_j - x_j^\top \beta)^2
+
\lambda |\beta|_1
$

它们可以控制过拟合。

Ridge 会压缩系数，Lasso 还能做稀疏选择。

------

## 树模型

例如 Random Forest、GBDT、XGBoost、LightGBM。

它们适合捕捉非线性关系，例如：

> 低估值在高质量股票中有效，但在低质量股票中无效。

这类关系线性模型不容易表达，但树模型可以自动分裂。

------

## 深度学习

可以用于：

- 时间序列预测。
- 新闻文本情绪。
- 订单簿建模。
- 多模态另类数据。
- 强化学习交易执行。

但在中低频股票选股中，深度模型未必天然优于简单模型，因为金融数据噪声大、非平稳、样本有效信息少。

所以量化里不是“模型越复杂越好”，而是：

$
\text{有效性} + \text{稳定性} + \text{可解释性} + \text{可交易性}
$

------

# 2.15 预测模型的输出不等于最终买卖

假设模型给出了预测收益：

$
\hat \mu_{t,i}
$

注意这只是预期收益，不是最终仓位。

从预测到仓位，中间还要经过：

1. 风险控制。
2. 行业约束。
3. 市值约束。
4. 流动性约束。
5. 交易成本约束。
6. 最大持仓限制。
7. 换手率限制。

所以量化策略不是：

$
\hat \mu_{t,i} \text{ 高} \Rightarrow \text{全仓买入}
$

而是：

$
\hat \mu_t \rightarrow \text{组合优化} \rightarrow w_t
$

其中：

- ^$(\hat \mu_t)$^：模型预测的收益向量。
- (w_t)：最终投资组合权重。

------

# 2.16 前沿因子处理：稳健性比复杂度更重要

现代量化不会只做标准化、中性化，还会关注因子在不同样本、不同状态和不同成本约束下是否稳定。

## 1. 横截面处理的升级

常见增强方法：

- 分行业标准化：先在行业内做 z-score，避免行业结构主导因子。
- 分市值桶标准化：在大盘、中盘、小盘内部比较。
- rank transform：把极端数值转成排序，降低异常值影响。
- Gaussian rank transform：把排序映射到正态分布，方便线性模型使用。
- residualization：对行业、市值、波动率、流动性等暴露回归取残差。

注意：中性化不是越多越好。过度中性化会把真实 alpha 一起剔除。实务中要比较：

```text
原始因子 -> 行业中性 -> 行业+市值中性 -> 多风险中性
```

看预测能力、换手和成本如何变化。

## 2. 标签处理的升级

普通未来收益标签：

```text
y = future_return
```

更实用的标签：

- 超额收益：扣除市场或基准收益。
- 行业中性收益：扣除行业共同涨跌。
- 残差收益：扣除风险模型解释部分。
- 排名标签：只学习横截面相对顺序。
- triple barrier 标签：考虑止盈、止损和最长持有期。
- 成本调整标签：`future_return - expected_cost`。

如果模型训练目标和交易目标不一致，模型指标可能很好但策略不赚钱。

## 3. 排序学习

选股更关心排序，而不是精确收益数值。可以使用：

- pairwise ranking：学习哪只股票应排在另一只前面。
- listwise ranking：直接优化一组股票的排序。
- LambdaRank / LambdaMART：在排序任务中常用，适合横截面选股。

评价指标不要只看 MSE，也要看：

```text
RankIC, top-k return, long-short spread, 分组单调性
```

## 4. 现代机器学习模型

实务中常见路线：

```text
线性模型 / Elastic Net
-> GBDT / LightGBM / XGBoost
-> TabNet / FT-Transformer 等表格深度模型
-> 时序模型 TCN / LSTM / Transformer
-> 多模态模型：行情 + 财报 + 文本 + 另类数据
```

经验判断：

- GBDT 在结构化因子上通常是强基线。
- 深度学习适合高频订单簿、文本、多模态和长序列。
- 线性模型在低信噪比场景下仍然很强，因为可解释、稳定、容易控制风险。
- 模型复杂度必须用样本外收益、成本后表现和稳定性证明。

## 5. 高频订单簿模型

高频预测常用输入：

```text
[bid_price_k, ask_price_k, bid_size_k, ask_size_k] for k=1...K
```

可以加上：

- 订单流不平衡 OFI。
- 主动成交方向。
- 撤单不平衡。
- 价差状态。
- 队列消耗速度。
- 波动率和成交强度。

常见模型：

- OFI 线性回归：高频强基线。
- Logistic/GBDT：预测下一跳方向。
- Hawkes 过程：建模订单、成交、撤单的自激发强度。
- Queue-reactive 模型：把买一/卖一队列状态作为核心状态。
- DeepLOB：CNN + 时序模型学习盘口结构。
- LOB Transformer：用 attention 学习较长事件依赖。

高频模型的标签：

- 下一跳 mid-price 方向。
- 固定秒数后的 mid-price return。
- 固定事件数后的 mid-price return。
- 限价单成交概率。
- 成交后的逆向选择损失。

## 6. Meta-labeling

Meta-labeling 的思想是把问题拆成两层：

```text
第一层：主模型决定是否存在交易机会和方向。
第二层：meta 模型判断这笔交易是否值得做、仓位该多大。
```

它适合处理：

- 信号方向有一定价值，但假信号很多。
- 想降低交易次数和换手。
- 想预测某笔交易的胜率、赔率或止损概率。

常用特征包括主模型分数、波动率、流动性、市场状态、成本、盘口状态等。

## 7. 模型训练的防泄漏框架

金融训练集不能随机切分。推荐：

- walk-forward：过去训练，未来验证，再滚动前进。
- purged K-fold：删除训练集中与验证标签窗口重叠的样本。
- embargo：验证集前后留出缓冲区，防止相邻样本泄漏。
- grouped split：按日期分组切分，而不是按样本随机切。

高频数据还要按交易日、股票和连续事件块切分，防止模型记住局部订单流。

# 2.17 一个更完整的多因子训练框架

```text
1. 定义股票池和可交易约束
2. 准备 point-in-time 数据
3. 构造原始因子和高频/另类特征
4. 去极值、标准化、中性化、方向统一
5. 构造和交易目标一致的标签
6. 使用 walk-forward 或 purged CV 训练
7. 用 IC、RankIC、分组收益和成本后收益评估
8. 对模型输出做风险暴露检查
9. 接入组合优化和交易成本模型
10. 做样本外、压力测试和实盘 shadow trading
```

核心 insight：

```text
模型不是研究终点。模型输出只有进入组合、成本、风险、执行之后仍然有效，才是 alpha。
```
